table ip filter {
    chain input {
        iifname { "vm-k8s-dev" } accept comment "Allow vm-k8s-dev to access the VM"
        iifname { "vm-k8s-dev" } icmp type { echo-request, destination-unreachable, time-exceeded } counter accept comment "Allow select ICMP"
        iifname { "vm-k8s-dev" } counter drop comment "Drop all other unsolicited traffic"
    }

    chain forward {
        iifname { "wg-k8s-dev" } accept comment "Allow to forward from wg-k8s-dev"
        iifname { "wg-k8s-dev" } counter drop comment "Drop all other unsolicited traffic"
    }
}

# vim: ft=nftables
