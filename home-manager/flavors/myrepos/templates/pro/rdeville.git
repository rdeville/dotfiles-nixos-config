include =
  CONF_DIR="${XDG_DATA_HOME:-${HOME}/.local/share}/mr/repos"
  cat ${CONF_DIR}/framagit.org/private/dotfiles.git
  cat ${CONF_DIR}/framagit.org/public/dotfiles.git
  cat ${CONF_DIR}/framagit.org/public/snippets.git
  if [[ -f "${CONF_DIR}/../hosts/palpatine/pro.git" ]]
  then
    cat ${CONF_DIR}/../hosts/palpatine/pro.git
  fi

# vim: ft=dosini
